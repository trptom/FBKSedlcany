<div class="well">
  <h3>TODO</h3>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>TODO</th>
        <th>TODO</th>
        <th>TODO</th>
      </tr>
    </thead>
    <tbody>
      <% for club in @clubs %>
      <tr>
        <td><%= club.name %></td>
        <td><%= club.get_teams.count %></td>
        <td class="btn-group">
          <%= link_to I18n.t("messages.base.show"), club, :class => "btn" %>
          <%= link_to I18n.t("messages.base.edit"), edit_club_path(club), :class => "btn" %>
          <% if club.get_teams.count == 0 %>
            <%= link_to I18n.t("messages.base.delete"), club,
                  :confirm => I18n.t("messages.clubs.index.delete_confirmation"),
                  :method => :delete,
                  :class => "btn" %>
          <% end %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <%= link_to I18n.t("messages.clubs.index.new_club"), new_club_path, :class => "btn" %>
</div>