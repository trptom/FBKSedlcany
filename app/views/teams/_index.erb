<div class="well">
  <h3>TODO</h3>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>TODO</th>
        <th>TODO</th>
        <th>TODO</th>
      </tr>
    </thead>
    <tbody>
      <% for team in @teams %>
      <tr>
        <td><%= team.name %></td>
        <td><%= team.players.count %></td>
        <td class="btn-group">
          <%= link_to I18n.t("messages.base.show"), team, :class => "btn" %>
          <%= link_to I18n.t("messages.base.edit"), edit_team_path(team), :class => "btn" %>
          <% if team.players.count == 0 %>
            <%= link_to I18n.t("messages.base.delete"), team,
                  :confirm => I18n.t("messages.teams.index.delete_confirmation"),
                  :method => :delete,
                  :class => "btn" %>
          <% end %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <%= link_to I18n.t("messages.teams.index.new_team"), new_team_path, :class => "btn" %>
</div>